# SPEC.md — 要求仕様: honyaku-query(翻訳探索クエリビルダー)

> **このファイルが仕様の正(Source of Truth)。** 実装は本ファイルの REQ-ID を根拠として行い、
> 記載のない機能を追加する場合は必ず本ファイルへの追記を先に提案すること。

## 1. 背景と目的

日本文学の海外語訳を探すとき、最大の障壁は日本人名・作品名のローマ字表記ゆれである
(Sōseki / Soseki / Souseki / Soseki Natsume / Natsume, S.)。本ツールは、著者名・作品名を
かな読み付きで入力すると、ヘボン式ローマ字化 → 表記バリアント展開 → 主要書誌データベース・
書店への検索 URL 一括生成を行う完全クライアントサイドツール。**外部 API は呼ばず
リンクアウトに徹する**ため運用コストゼロ・オフラインでクエリ構築可能。
翻訳探索3部作 その1(ローマ字化・バリアント展開モジュールは案2で再利用される前提で設計する)。

## 2. スコープ

### In Scope
- かな(ひらがな/カタカナ)→ ヘボン式ローマ字変換
- 表記バリアント展開(長音表記・姓名順・イニシャル形)と優先順位付け
- 検索ターゲット(書誌 DB・図書館・書店)マスタと検索 URL 生成
- 対象言語による検索ターゲットのフィルタ
- 検索セットの URL 共有、最近の検索の localStorage 保存

### Out of Scope(明示的にやらないこと)
- 検索結果の取得・表示(各サイトへのリンクアウトのみ。API 呼び出し・スクレイピングをしない)
- 書誌データの蓄積(honyaku-atlas の領分)
- 訓令式・日本式ローマ字(v1 はヘボン式のみ。海外書誌の実態に合わせる)
- 漢字からの読み推定(読みはユーザが入力する。形態素解析エンジンを載せない)

## 3. 用語定義

| 用語 | 定義 |
|---|---|
| 読み | ユーザが入力するかな表記(姓・名・作品名それぞれ)。ひらがな/カタカナは同一視 |
| 基本形 | ヘボン式変換の第一候補。長音はマクロン(ō, ū)で表記 |
| バリアント | 基本形から規則的に展開される別表記。展開規則ごとに優先度を持つ |
| 展開規則 | (a) 長音: ō → o / ou / oh、ū → u / uu(位置により)(b) 姓名順: 姓-名 / 名-姓 (c) イニシャル: 名をイニシャル化 (d) 促音・撥音の代替は行わない(ヘボン式内で一意のため) |
| 検索ターゲット | {id, 名称, 言語, URLテンプレート, クエリエンコード方式} を持つリンク先マスタ |

## 4. 機能要求

| ID | 要求 | 優先度 | 受入条件(Acceptance Criteria) | 状態 |
|---|---|---|---|---|
| REQ-001 | かな→ヘボン式変換ができる | Must | ・純粋関数 `toHepburn(kana) => Result<string>`。ひらがな/カタカナ入力を同一結果に<br>・拗音(しゃ→sha, ちゅ→chu, じょ→jo)、促音(っk→kk、ただし っち→tchi)、撥音(ん→n。ただし b/m/p の前は m: しんぶん→shimbun)、ん+母音/や行は n'(しんいち→shin'ichi)<br>・長音: おう/おお→ō、うう→ū、えい→ei(eiは長音化しない)。カタカナ長音符「ー」は直前母音のマクロン<br>・変換不能文字(漢字・記号)は `unconvertible` エラーで位置を返す<br>・変換表は `src/core/hepburn.ts` にデータとして持ち、規則(促音・撥音・長音)はロジックで実装 | **完了** |
| REQ-002 | 表記バリアントが展開される | Must | ・入力: {姓(基本形), 名(基本形)}。出力: 優先度順のバリアント列<br>・長音展開: マクロン形 → プレーン形(ō→o)→ ou 形 → oh 形(語末のみ)の優先順<br>・姓名順: 「名 姓」(西洋順)を主、「姓 名」を副とする。イニシャル形(名→頭文字+ピリオド)は末尾優先度<br>・重複は除去し、**上限 12 件**(超過分は優先度順に切り捨て、切り捨てた旨を返す)<br>・作品名は長音展開のみ(姓名順・イニシャルは適用しない)<br>・展開は決定的(同一入力→同一出力列) | **完了** |
| REQ-003 | 検索ターゲットマスタと URL 生成 | Must | ・`src/core/targets.ts` に静的定数 10 件以上: Wikidata / Open Library / WorldCat / Internet Archive / 国立国会図書館サーチ / 国際交流基金 翻訳データベース / Google Books / 各国書店(米英独仏あたり)等<br>・各ターゲット: {id, name, langs(対応言語), urlTemplate("{q}" 置換), encoding: percent | plus}<br>・URL 生成はダイアクリティカルマーク(ō 等)を正しくパーセントエンコードする<br>・urlTemplate の妥当性(https・{q} を含む)を検証関数が保証(ビルド時エラー)<br>・verified_at を持ち、365 日超は「リンク要確認」表示 | 未着手 |
| REQ-004 | 入力→バリアント→リンク一覧の UI | Must | ・姓・名・作品名(いずれも任意、最低1フィールド)を入力すると、バリアント一覧と、選択バリアント×ターゲットのリンク集が表示される<br>・バリアントは個別に ON/OFF できる(既定は優先度上位 4 件 ON)<br>・リンクは新規タブで開く。コピー用のクエリ文字列一括コピー機能を持つ | 未着手 |
| REQ-005 | 対象言語でターゲットを絞れる | Should | ・言語選択(en/fr/de/es/it/zh/ko/multi)でターゲットをフィルタ。multi(横断DB)は常に表示<br>・選択状態は URL クエリと同期 | 未着手 |
| REQ-006 | 検索セットを URL で共有できる | Should | ・{姓読み, 名読み, 作品読み, 言語, ONバリアント} をハッシュにシリアライズ、round-trip 保証<br>・不正ハッシュは空状態で起動(クラッシュしない) | 未着手 |
| REQ-007 | 最近の検索が保存される | Could | ・直近 10 件を localStorage に保存・一覧から再実行。storage 不可環境でも機能低下のみ | 未着手 |

## 5. 非機能要求

| ID | 分類 | 要求 |
|---|---|---|
| NFR-001 | 品質 | `src/core/` のカバレッジ 90% 以上、全体 80% 以上 |
| NFR-002 | 構成 | ランタイムの外部 API 呼び出しゼロ(リンクアウトのみ)。オフラインでクエリ構築まで動作 |
| NFR-003 | 性能 | 初回ロード JS 150KB(gzip)以下 |
| NFR-004 | 再利用 | hepburn.ts / variants.ts は honyaku-atlas から import 可能な純度で実装する(DOM・ターゲットマスタへの依存禁止) |
| NFR-005 | 表示 | 「ヘボン式変換・バリアントは機械的な近似であり、著者の公式ローマ字表記と異なる場合があります」を表示 |

## 6. データ・外部依存

- 入力: ユーザ入力(かな読み)、URL ハッシュ、localStorage
- 出力: 検索 URL(リンクアウト)、画面表示
- 外部 API / サービス: なし(リンク先の可用性には依存するが呼び出さない)

## 7. 制約

- TypeScript / Vite / Vitest / Biome(AGENTS.md 参照)
- 文字列処理は Unicode 正規化(NFC)を入力直後に1回行い、以降は正規化済み前提で扱う

## 8. 変更履歴

| 日付 | 変更 | REQ-ID | 起票者 |
|---|---|---|---|
| 2026-07-03 | 初版作成 | REQ-001〜007 | 哲朗 |
